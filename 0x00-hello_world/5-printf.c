#include <stdio.h>
/**
 * main - program that uses the printf function
 * Return: 0 if the program is a success
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
